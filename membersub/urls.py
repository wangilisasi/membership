from django.urls import path, include
from . import views
app_name = 'membersub'
urlpatterns = [
       # path('memberships/', views.MembershipView.as_view(), name="memberships"),
       # path('register/', views.sign_up_user_view, name="signup"),
       
]

